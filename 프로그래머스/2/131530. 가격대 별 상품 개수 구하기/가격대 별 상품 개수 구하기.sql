-- 코드를 입력하세요
# SET @PR = -10000;
# SELECT @PR := @PR + 10000 AS PRICE_GROUP, (
#         SELECT COUNT(*) FROM PRODUCT WHERE PRICE BETWEEN @PR AND (@PR + 9999)) AS PRODUCTS
# FROM PRODUCT

SELECT FLOOR(PRICE / 10000) * 10000 AS PRICE_GROUP, COUNT(*) AS PRODUCTS
FROM PRODUCT
GROUP BY FLOOR(PRICE / 10000)
ORDER BY FLOOR(PRICE/10000) ASC